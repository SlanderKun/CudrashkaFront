import React from 'react';
import {observer} from "mobx-react-lite";

const PromocodesSection: React.FC = () => {
    return (
        <div>

        </div>
    );
};

export default observer(PromocodesSection);