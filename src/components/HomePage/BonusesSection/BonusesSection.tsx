import React from 'react';
import {observer} from "mobx-react-lite";

const BonusesSection:React.FC = () => {
    return (
        <div>

        </div>
    );
};

export default observer(BonusesSection);